<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="bot.views.ItemStats.ItemJoyeria">
    <VerticalStackLayout>
        <Label 
          Text="{Binding ItemId, StringFormat='Phy. absorption {0}'}"
     FontSize="16"
    VerticalOptions="Center" 
    HorizontalOptions="Start" />
        <Label 
      Text="{Binding ItemId, StringFormat='Mag. absorption {0}'}"
 FontSize="16"
VerticalOptions="Center" 
HorizontalOptions="Start" />
    </VerticalStackLayout>
</ContentView>
